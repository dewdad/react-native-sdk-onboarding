/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { AppRegistry } from 'react-native';
import HyperTrackOnboarding from './app/index.js';

AppRegistry.registerComponent('HyperTrackOnboarding', () => HyperTrackOnboarding);
